<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".LaundryBuddyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaundryBuddy"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Splash / Login Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.LaundryBuddy.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Signup Activity -->
        <activity
            android:name=".ui.auth.SignupActivity"
            android:exported="false"
            android:theme="@style/Theme.LaundryBuddy" />

        <!-- Main Activity with Bottom Navigation -->
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.LaundryBuddy" />

        <!-- Submit Order Activity -->
        <activity
            android:name=".ui.orders.SubmitOrderActivity"
            android:exported="false"
            android:parentActivityName=".ui.home.MainActivity"
            android:theme="@style/Theme.LaundryBuddy" />

        <!-- Contact Activity -->
        <activity
            android:name=".ui.support.ContactActivity"
            android:exported="false"
            android:parentActivityName=".ui.home.MainActivity"
            android:theme="@style/Theme.LaundryBuddy" />

        <!-- Staff Dashboard Activity -->
        <activity
            android:name=".ui.staff.StaffDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.LaundryBuddy" />

        <!-- Reset Password Activity -->
        <activity
            android:name=".ui.auth.ResetPasswordActivity"
            android:exported="false"
            android:parentActivityName=".ui.auth.LoginActivity"
            android:theme="@style/Theme.LaundryBuddy.NoActionBar" />

        <!-- Privacy Policy Activity -->
        <activity
            android:name=".ui.legal.PrivacyPolicyActivity"
            android:exported="false"
            android:theme="@style/Theme.LaundryBuddy" />

        <!-- Terms of Service Activity -->
        <activity
            android:name=".ui.legal.TermsOfServiceActivity"
            android:exported="false"
            android:theme="@style/Theme.LaundryBuddy" />

        <!-- QR Scanner Activity -->
        <activity
            android:name=".ui.scanner.QrScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.LaundryBuddy.Dark.NoActionBar" />

        <!-- FileProvider for sharing exported files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

    </application>

</manifest>
